我需要你制作一个AI对话客户端，要支持安卓4.4.2及以上，请必须保持流畅度美观度和兼容性。
具体要求如下：
1.深浅色模式切换
2.侧边栏查看历史记录还有设置按钮以及新对话按钮，历史记录支持增删改查
3.首页底部就是对话框，可以选择模型，是否开启联网搜索，联网搜索的条数等等。还有上传文件，包括图片和文档。
4.设置页需要包含：设置模型，需要填入统一openai兼容型API，需要baseurl，apikey，modelname，还有模型昵称。支持添加多个来源的模型，还有同一来源多个模型，还有设置模型是否为多模态。还有一个板块设置tavily联网搜索的baseurl和apikey，支持添加多个key进行轮询。
5.用户发出的对话框底下要有编辑信息并重发，复制，重试3个图标。AI的回复框底下要有编辑，复制，重试3个图标，记住是图标不是文字按钮。整个要完整的支持显示所有md格式，一定要全。
6.多模态上传直接通过标准通道，纯文本模型先解析文字，后发送。
7.支持导入json格式的模型配置，联网搜索配置，和系统其他配置。支持导入导出历史记录，所有模型，联网搜索，系统设置的json文件。可自由选择导出哪些，可多选。
8.支持api轮询，下面的要求只用于AI模型，不适用于联网搜索设置，就是首先用户创建一个组，组里面可以设置多个提供商和apikey，对话界面仅显示组名，程序自动随机调用组内，不通重试1次，然后换下一个提供商。一般一组是统一个模型，但不强求。
项目名：Nostalgia-AI
项目地址：https://github.com/HaiDianFirstTeam/Nostalgia-AI
我需要通过github action自动编译release，并不本地编译，通过tag管理。请提供首次完整教程和代码，和后续上传新版本代码的教程和代码。写进单独的GitHub编译.md文档。
另外需要中文的readme.md文档。