问题：
2.添加终止输出按钮，在发送后原本发送按钮的位置，变成停止按钮，模型输出完后再变回来。
3.底部的输入框及上面功能按钮，会挡住模型的输出，还有输出框底下的按钮，以及下一次用户发送出去的提示词框，请修复。
4.所有配置在更改后，现在的版本有bug，必须新建对话才能使用最新配置，这是不对的。应该配置是实时同步的，如果我原有模型A，在开启新对话对话了1轮后，加入模型B，那么此时我应该可以选择模型B，在这个对话里面。现在的版本就不可以。
5.你需要在设置里支持流式输出，支持切换3种模式：流式输出，兼容型流式输出（即以每n秒的速度（支持设置），输出模型在期间的一段话，以此减少老旧设备连续流式输出卡死的问题。），关闭流式输出。
6.联网搜索数量不应该有上限，另外如开启联网搜索，需要在模型回复框的上面，有几个小小的横向的扁扁的胶囊形状的按钮，每个按钮对应一个链接。按钮是该网页的名字，点击后可跳转到对应的链接。搜索几个就列出几个胶囊，注意要细长的，节省空间。如果超过5个则折叠起来，点击位于他们下方，AI输出框上方的下箭头展开显示所有联网搜索的胶囊。
7.现在的配置导出支持2种模式，一种是文件导出，需要弹窗显示导出位置。另一种是剪贴板导出，自动把当前配置导出到剪切板，用户可复制粘贴。导入也是支持文件导出和剪切板导入
8.在高版本安卓（安卓12上无法显示图标），在安卓4上可以显示图标。图标文件是根目录/icon.png
9.低版本安卓安装时，导入配置文件无法导入，所有文件均为灰色，不可选择。高版本正常，可自动过滤所有非json文件，并导入json.